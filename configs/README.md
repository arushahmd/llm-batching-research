# Experiment Configurations

- `group1/` — semantic/random mini-batch composition at 1K, 3K, and 5K
- `group2/` — easy-to-hard/hard-to-easy length ordering at 1K, 3K, and 5K

The paired configuration families share the matched model, data split, LoRA,
training-budget, seed, and evaluation settings documented in
`docs/experiment-protocol.md`.
